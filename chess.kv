#:import dp kivy.metrics.dp
#:kivy 2.3

ChessGame:

    BoardGrid:
        id: BoardGrid

        canvas.before:
            Rectangle:
                size: self.board_size, self.board_size
                source: "images/Board.png"

    Ui:

        orientation: "vertical"
        size_hint: None, None
        size: self.ui_size_x, self.ui_size_y

        canvas.before:
            Color:
                rgba: .7, .7, .7, 1
            Rectangle:
                pos: self.pos
                size: self.size

        ScrollView:
            size_hint: 1, 1

            Label:
                text: BoardGrid.move_list_text
                text_size: self.width, None
                size_hint_y: None
                height: max(self.texture_size[1], self.parent.height)
                color: 0, 0, 0, 1
                halign: "left"
                valign: "top"